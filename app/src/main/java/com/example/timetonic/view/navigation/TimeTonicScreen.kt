package com.example.timetonic.view.navigation

enum class TimeTonicScreen(val route: String) {
    Login(route = "login"),
    Landing(route = "landing"),
    Detail(route = "detail"),
    BookId(route = "bookId")
}