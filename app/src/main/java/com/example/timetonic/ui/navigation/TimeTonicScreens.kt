package com.example.timetonic.ui.navigation

enum class TimeTonicScreens(val route: String) {
    Login(route = "login"),
    Landing(route = "landing"),
    SessionKey(route = "sesskey"),
    O_U(route = "o_u"),
    B_C(route = "b_c"),
    Detail(route = "detail"),
}